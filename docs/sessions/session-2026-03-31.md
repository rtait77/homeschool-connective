---
name: Session 2026-03-31 — Mobile Audit + Site-Wide Fixes
description: Full site audit and mobile fix session — auth race, consulting redesign, learn page, navbar, printables, blog images, admin intake form
type: project
---

## Changes Made

### Auth Fix (dashboard + account pages)
- Replaced `getUser()` / `getSession()` with `onAuthStateChange` + `INITIAL_SESSION` event
- Fires as soon as Supabase initializes (no more blank screen on first click)
- Pattern: listen for INITIAL_SESSION or SIGNED_IN, call load(user), then unsubscribe

### Consulting Page (`app/consulting/page.tsx`)
- Full teal redesign: hero bg `#238FA4`, step circles `#238FA4`, icon bg `#edf7fa`, SVGs `stroke="#238FA4"`
- Pricing block removed — replaced with plain info list (no price shown)
- Terms & agreement block removed entirely
- "See Pricing →" button links to `/pricing` (teal `#238FA4`)
- How It Works: 2-col grid on mobile via CSS media query

### Pricing Page (`app/pricing/page.tsx`)
- Cards now equal height (`items-stretch`)
- Consulting card: border + price → teal `#238FA4`, shadow updated
- Consulting button: always "Book a Consult →", opens modal (not /signup)
- Modal: checkboxes + confirm button → teal `#238FA4`
- Consulting terms added to modal (shows only when target === 'consulting')

### Learn Page (`app/learn/page.tsx`)
- Free game badges: `Solar System Sizzle` → `freeLabel: 'Free Demo'`, `Ordering the Planets` → `freeLabel: 'Free'`
- Free games always accessible (no auth gate), show badge only when logged out
- Removed all `mini: true/false` fields
- Printable card: image container `bg-white`, Get PDF stacks below title (`flex-col gap-1`)
- Filter labels: `w-full sm:w-auto` so chips start on own line on mobile
- Removed "35+" from printables message; "in the works" → "in discussion"

### Navbar (`components/layout/navbar-preview.tsx`)
- Dashboard pill = orange (`#ed7c5a`) for regular users → `/dashboard`
- Admin pill = orange for admin users → `/admin`, text "Admin Panel"
- Removed "Admin Panel" from profile dropdown (desktop + mobile)

### Homepage (`app/page.tsx`)
- Consulting block: resource count + "Learn More" button → teal `#238FA4`
- Printable cards: image container `bg-white`, Get PDF stacks below title

### Dashboard (`app/dashboard/page.tsx`)
- "Games — Active" badge: teal → orange `#ed7c5a`
- "Consulting — Active" badge: outline → solid teal `bg-[#55b6ca] text-white`

### About Page (`app/about/page.tsx`)
- Rebecca + Melanie: title shows inline on desktop, block below name on mobile (responsive spans)

### Admin (`app/admin/page.tsx`)
- Tab bar: wrapped in `overflow-x: auto` for horizontal scroll on mobile
- "Results at a Glance": grid → `repeat(auto-fit, minmax(120px, 1fr))`
- Curriculum Recommendations header: `flexWrap: 'wrap'`
- `DarkSummaryRow`: label `minWidth` 185px → 130px, added `flexWrap: 'wrap'`, `overflowWrap: 'anywhere'` on value

### Blog Listing (`app/tips/page.tsx`)
- Card images: `aspectRatio: '16/9'`, `object-contain` (no more cropping)

### Blog Post Pages (all 6)
- Hero banner: removed `h-64` + `fill` + `object-cover`
- Now: `<Image width={1200} height={400} className="w-full h-auto" />` — full image, no crop

---

## Session 2 — QA Testing + Sidebar Nav Redesign

### Bug Fixes (from full customer flow QA)
- **`app/auth/callback/route.ts`** — redirects to `/auth/confirm` (was `/learn`) so auth state loads before user lands
- **`app/auth/confirm/page.tsx`** — NEW: client component, waits for `onAuthStateChange` session, then redirects to `/learn` with spinner
- **`app/subscribe/success/page.tsx`** — now waits for auth session before showing "Start Learning →" button (was static)
- **`app/api/webhook/route.ts`** — consulting buyers now get `subscription_status: 'trialing'` + `trial_end` set (was broken)
- **`app/api/cron/route.ts`** — trial reminder email link fixed from `/subscribe` (404) to `/pricing`
- **SMTP** — switched Supabase auth email sender from smtp.sender.net (broken) to smtp.titan.email (working)

### Dashboard + Account Sidebar Nav
- **`components/dashboard/sidebar.tsx`** — NEW: shared left sidebar with Dashboard + Account Settings links, active state in coral
- **`app/dashboard/page.tsx`** — wrapped with `DashboardSidebar`, try/catch/finally added so loading state never hangs
- **`app/account/page.tsx`** — wrapped with `DashboardSidebar`, removed "← Back to Dashboard" link, white background + cream cards

### Navbar Redesign
- **`components/layout/navbar-preview.tsx`** — pill nav now shows for ALL users (Learn + Tips)
- Logged-in users: pill nav + orange "Dashboard" button (same shape as "Start Free Trial" CTA)
- Avatar dropdown: email + Log Out only (removed Dashboard + Account Settings links)

### Account Page Styling
- Background: cream `#f5f1e9` → white
- Cards: white → cream `#f5f1e9`
- Input disabled state: `#faf8f5` → `#f0ece6`

### Process
- Stripe is live mode only (no test environment) — test cards don't work; used real card + refunded
- Session log feedback memory created; logs now pushed to GitHub at `docs/sessions/`

---

## Session 3 — Consulting Flow + Nav/Dashboard Polish

### First Name Field
- `app/signup/page.tsx` — added First Name field, passes via `options.data.first_name`
- `app/auth/callback/route.ts` — saves `first_name` from `user_metadata` to profiles on confirm
- `app/dashboard/page.tsx` — loads `first_name` from profile, shows "Welcome back, [name]!" with email prefix fallback
- `app/account/page.tsx` — added "YOUR NAME" card to edit first name, saves to profiles table
- Supabase: `first_name text` column added to `profiles` table (nullable)

### Dashboard Auth Fix
- `app/dashboard/page.tsx` — switched from `getUser()` to `onAuthStateChange` (fixes intermittent loading hang)

### Sidebar Nav Polish
- `components/dashboard/sidebar.tsx` — orange text active state (no filled pill), "MENU" label, right border + shadow divider
- `app/account/page.tsx` — white background, cream cards

### Navbar Updates
- Dashboard pill link active when on `/account` too (`isActive('/dashboard')` checks both paths)
- Contact added to logged-in dropdown
- Pill nav: Learn | Tips | Dashboard (orange, separated by divider) for logged-in users
- Dropdown: email + Contact + Log Out only

### Trial Expiry Fix
- Dashboard: "Subscribe to keep access →" removed during active trial; only shows after trial ends with "Trial ended" red badge

### Consulting Signup Flow (new users can pay without pre-existing account)
- `app/consulting/signup/page.tsx` — NEW: first name + email + password → creates account → logs in → Stripe
- `app/api/consulting-account/route.ts` — NEW: admin creates user (auto-confirmed), saves first_name to profile
- `app/consulting/page.tsx` — restored teal hero (`#238FA4`), removed pricing/terms section, "Book a Consult →" links to `/pricing`, "See Pricing →" button after How It Works
- `app/pricing/page.tsx` — "Book a Consult →" modal confirm redirects to `/consulting/signup` if not logged in
- `app/api/checkout-consulting/route.ts` — auth optional, pre-fills email if logged in, `customer_creation: 'always'`
- `app/api/webhook/route.ts` — creates Supabase account if none exists for consulting buyer, generates password reset link, includes in welcome email, saves first_name from Stripe name

### Session Logging
- Feedback memory saved: create/update session log each session, push to `docs/sessions/` on GitHub

## Still Pending
- Asteroid blast games on mobile — game canvas only fills top ~40% of screen (complex HTML, deferred)
- Consulting terms in user dashboard + confirmation email (on TODO list in project_roadmap.md)
- Pricing page: `showTrial` flash — brief orange CTA flash for expired trial users before Supabase check completes
- Test Flow 2 ($50/yr) + Flow 3 ($47 consulting) still need to be completed
- Clean up test users (test1–test5) from Supabase
- Confirmation email template still shows bare Supabase default
